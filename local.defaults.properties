# Fallback used by the secrets-gradle-plugin when local.properties has no
# MAPS_API_KEY (e.g. in CI). Replace with a real key locally via
# local.properties to get working Google Maps tiles.
MAPS_API_KEY=DUMMY_MAPS_API_KEY
